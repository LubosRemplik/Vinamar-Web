# K — E-maily Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Zavést sadu česky psaných transakčních e-mailů (HTML) kolem životního cyklu rezervace — host i majitel dostanou zprávu při poptávce, potvrzení, odmítnutí, zrušení a 14 dní před příjezdem.

**Architecture:** Onion. Doménový port `GuestNotifier` (+ rozšíření `OwnerNotifier`) volají aplikační handlery po zápisu stavu. Infrastruktura: dva SMTP adaptéry (guest/owner) sdílející modul HTML šablon (kostra Postmark `basic-full`, render čistými TS funkcemi, bez šablonovacího enginu). Připomínka jede denním cronem (`@nestjs/schedule`) s idempotencí přes nový sloupec `inquiries.arrival_reminder_sent_at`.

**Tech Stack:** NestJS 11, `@nestjs/cqrs`, `@nestjs/schedule`, `nodemailer` (vše už v projektu), raw SQL přes `pg`, migrace `node-pg-migrate`, testy `jest` + `ts-jest`.

## Global Constraints

- Všechny e-maily **pouze v češtině**, s diakritikou.
- E-mail je **best-effort**: jeho selhání nesmí shodit příkaz ani rollbacknout stav rezervace → `try/catch` + `logger.warn`, handler doběhne úspěšně.
- Žádná nová těžká závislost (žádný šablonovací engine) — render čistými TS funkcemi.
- HTML kostra vychází z Postmark `basic-full` (MIT). Akcentová barva konstanta `#2563eb` (finální branding až bod L).
- Datum formátováno česky v UTC: `14. 7. 2025`.
- Handlery nikdy neflushují ručně mimo repository; drží se stávajícího raw-SQL vzoru.
- Spec: `docs/superpowers/specs/2026-06-21-k-emaily-design.md`.

---

## File Structure

**Nové soubory:**
- `api/src/domain/inquiry/guest-notifier.port.ts` — port `GuestNotifier` + symbol `GUEST_NOTIFIER`.
- `api/src/infrastructure/notify/templates/base.ts` — `baseLayout()`, `formatCzechDate()`, typ `EmailContent`, konstanty brandingu.
- `api/src/infrastructure/notify/templates/messages.ts` — 5 render funkcí vracejících `EmailContent`.
- `api/src/infrastructure/notify/smtp-guest-notifier.ts` — `SmtpGuestNotifier implements GuestNotifier`.
- `api/src/application/inquiry/send-arrival-reminders.command.ts`
- `api/src/application/inquiry/send-arrival-reminders.handler.ts`
- `api/src/infrastructure/notify/arrival-reminder.cron.ts`
- `api/migrations/1700000006000_inquiry-arrival-reminder.sql`

**Měněné soubory:**
- `api/src/domain/inquiry/owner-notifier.port.ts` — přidat `bookingCancelled`.
- `api/src/domain/inquiry/inquiry.repository.port.ts` — přidat `listDueForArrivalReminder`, `markArrivalReminderSent`.
- `api/src/infrastructure/persistence/pg-inquiry.repository.ts` — implementace nových metod.
- `api/src/infrastructure/notify/smtp-owner-notifier.ts` — přidat `bookingCancelled` (HTML šablona).
- `api/src/application/inquiry/submit-inquiry.handler.ts` — guest maily.
- `api/src/application/inquiry/confirm-inquiry.handler.ts` — guest maily.
- `api/src/application/inquiry/decline-inquiry.handler.ts` — guest mail.
- `api/src/application/availability/cancel-calendar-entry.handler.ts` — guest + owner mail.
- `api/src/interface/inquiry.module.ts` — DI wiring (porty, adaptéry, cron, handler).
- `api/test/fakes/index.ts` — `SpyGuestNotifier`, rozšíření `SpyNotifier` a `InMemoryInquiries`.
- `api/test/application/submit-inquiry.handler.spec.ts`, `confirm-inquiry.handler.spec.ts`, `cancel-calendar-entry.handler.spec.ts` — nové konstruktory + asserty.
- `docker-compose.yml`, `.env.example`, `README.md` — nové env proměnné.

---

## Task 1: Modul šablon (čisté render funkce)

**Files:**
- Create: `api/src/infrastructure/notify/templates/base.ts`
- Create: `api/src/infrastructure/notify/templates/messages.ts`
- Test: `api/test/infrastructure/email-templates.spec.ts`

**Interfaces:**
- Produces:
  - `type EmailContent = { subject: string; html: string; text: string }`
  - `baseLayout(opts: { preheader: string; content: string; cta?: { label: string; url: string } }): string`
  - `formatCzechDate(d: Date): string` → `"14. 7. 2025"`
  - `inquiryReceivedEmail(inquiry: Inquiry): EmailContent`
  - `bookingConfirmedEmail(inquiry: Inquiry): EmailContent`
  - `inquiryDeclinedEmail(inquiry: Inquiry): EmailContent`
  - `bookingCancelledEmail(inquiry: Inquiry, opts: { isOwner: boolean }): EmailContent`
  - `arrivalReminderEmail(inquiry: Inquiry): EmailContent`

- [ ] **Step 1: Napiš padající test**

`api/test/infrastructure/email-templates.spec.ts`:

```ts
import { EmailAddress } from '../../src/domain/shared/email-address';
import { DateRange } from '../../src/domain/shared/date-range';
import { Inquiry } from '../../src/domain/inquiry/inquiry';
import {
  formatCzechDate,
  inquiryReceivedEmail,
  bookingConfirmedEmail,
  inquiryDeclinedEmail,
  bookingCancelledEmail,
  arrivalReminderEmail,
} from '../../src/infrastructure/notify/templates/messages';

const inquiry = () =>
  new Inquiry(
    'id-1',
    'Jan Novák',
    new EmailAddress('jan@x.cz'),
    '+420777111222',
    new DateRange(new Date('2025-07-14'), new Date('2025-07-25')),
    'ahoj',
    'confirmed',
    new Date('2025-06-01'),
  );

describe('formatCzechDate', () => {
  it('formats as Czech D. M. YYYY in UTC', () => {
    expect(formatCzechDate(new Date('2025-07-14'))).toBe('14. 7. 2025');
  });
});

describe('email templates', () => {
  it('inquiryReceived: subject + greeting + stay dates in HTML, plus text fallback', () => {
    const m = inquiryReceivedEmail(inquiry());
    expect(m.subject).toContain('poptávku');
    expect(m.html).toContain('Jan Novák');
    expect(m.html).toContain('14. 7. 2025');
    expect(m.html).toContain('25. 7. 2025');
    expect(m.html).toContain('<!doctype html>');
    expect(m.text).toContain('Jan Novák');
  });

  it('bookingConfirmed mentions confirmation', () => {
    expect(bookingConfirmedEmail(inquiry()).subject.toLowerCase()).toContain('potvrzena');
  });

  it('inquiryDeclined is polite and mentions the term', () => {
    expect(inquiryDeclinedEmail(inquiry()).html).toContain('14. 7. 2025');
  });

  it('bookingCancelled has distinct guest vs owner subject', () => {
    const guest = bookingCancelledEmail(inquiry(), { isOwner: false });
    const owner = bookingCancelledEmail(inquiry(), { isOwner: true });
    expect(guest.subject).not.toBe(owner.subject);
    expect(owner.html).toContain('Jan Novák');
  });

  it('arrivalReminder is minimal and mentions the upcoming stay', () => {
    expect(arrivalReminderEmail(inquiry()).html).toContain('14. 7. 2025');
  });
});
```

- [ ] **Step 2: Spusť test, ověř že padá**

Run: `cd api && npm test -- email-templates`
Expected: FAIL — `Cannot find module '.../templates/messages'`.

- [ ] **Step 3: Implementuj `base.ts`**

`api/src/infrastructure/notify/templates/base.ts`:

```ts
export type EmailContent = { subject: string; html: string; text: string };

// Branding placeholders — finální hodnoty přijdou v bodě L.
const BRAND_NAME = 'Vinamar';
const BRAND_TAGLINE = 'Vinamar — Apartmán La Mata, Torrevieja';
const ACCENT = '#2563eb';

export function publicBaseUrl(): string {
  return process.env.PUBLIC_BASE_URL ?? '#';
}

// Formát je čistě číselný (žádné názvy měsíců), proto stačí ruční složení v UTC.
export function formatCzechDate(d: Date): string {
  return `${d.getUTCDate()}. ${d.getUTCMonth() + 1}. ${d.getUTCFullYear()}`;
}

function ctaBlock(cta?: { label: string; url: string }): string {
  if (!cta) return '';
  return `
  <table role="presentation" border="0" cellpadding="0" cellspacing="0" class="btn btn-primary">
    <tbody><tr><td align="left"><table role="presentation" border="0" cellpadding="0" cellspacing="0">
      <tbody><tr><td><a href="${cta.url}" target="_blank">${cta.label}</a></td></tr></tbody>
    </table></td></tr></tbody>
  </table>`;
}

// Kostra Postmark `basic-full` (MIT) přepsaná do TS; bloky Twigu = parametry.
export function baseLayout(opts: {
  preheader: string;
  content: string;
  cta?: { label: string; url: string };
}): string {
  return `<!doctype html>
<html lang="cs">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <title>${BRAND_NAME}</title>
    <style media="all" type="text/css">
      body { font-family: Helvetica, sans-serif; font-size: 16px; line-height: 1.3; background-color: #f4f5f6; margin: 0; padding: 0; }
      table { border-collapse: separate; width: 100%; }
      table td { font-family: Helvetica, sans-serif; font-size: 16px; vertical-align: top; }
      .body { background-color: #f4f5f6; width: 100%; }
      .container { margin: 0 auto !important; max-width: 600px; padding-top: 24px; width: 600px; }
      .content { box-sizing: border-box; display: block; margin: 0 auto; max-width: 600px; }
      .main { background: #ffffff; border: 1px solid #eaebed; border-radius: 16px; width: 100%; }
      .wrapper { box-sizing: border-box; padding: 24px; }
      .header { padding: 24px 0 12px; text-align: center; }
      .header a { color: ${ACCENT}; font-size: 24px; font-weight: bold; text-decoration: none; }
      .footer { clear: both; padding-top: 24px; text-align: center; width: 100%; }
      .footer td, .footer p, .footer span, .footer a { color: #9a9ea6; font-size: 14px; text-align: center; }
      p { font-family: Helvetica, sans-serif; font-size: 16px; margin: 0 0 16px; }
      a { color: ${ACCENT}; text-decoration: underline; }
      .btn { box-sizing: border-box; min-width: 100% !important; width: 100%; }
      .btn > tbody > tr > td { padding-bottom: 16px; }
      .btn table { width: auto; }
      .btn table td { background-color: #ffffff; border-radius: 4px; text-align: center; }
      .btn a { background-color: #ffffff; border: solid 2px ${ACCENT}; border-radius: 4px; box-sizing: border-box; color: ${ACCENT}; cursor: pointer; display: inline-block; font-size: 16px; font-weight: bold; padding: 12px 24px; text-decoration: none; }
      .btn-primary table td { background-color: ${ACCENT}; }
      .btn-primary a { background-color: ${ACCENT}; border-color: ${ACCENT}; color: #ffffff; }
      .preheader { color: transparent; display: none; height: 0; max-height: 0; max-width: 0; opacity: 0; overflow: hidden; mso-hide: all; visibility: hidden; width: 0; }
      @media only screen and (max-width: 640px) {
        .main p, .main td, .main span { font-size: 16px !important; }
        .wrapper { padding: 8px !important; }
        .container { padding: 0 !important; padding-top: 8px !important; width: 100% !important; }
        .main { border-left-width: 0 !important; border-radius: 0 !important; border-right-width: 0 !important; }
        .btn table, .btn a { max-width: 100% !important; width: 100% !important; }
      }
      .apple-link a { color: inherit !important; font-family: inherit !important; font-size: inherit !important; font-weight: inherit !important; line-height: inherit !important; text-decoration: none !important; }
      #MessageViewBody a { color: inherit; text-decoration: none; font-size: inherit; font-family: inherit; font-weight: inherit; line-height: inherit; }
    </style>
  </head>
  <body>
    <table role="presentation" border="0" cellpadding="0" cellspacing="0" class="body"><tr>
      <td>&nbsp;</td>
      <td class="container"><div class="content">
        <span class="preheader">${opts.preheader}</span>
        <div class="header"><a href="${publicBaseUrl()}">${BRAND_NAME}</a></div>
        <table role="presentation" border="0" cellpadding="0" cellspacing="0" class="main"><tr>
          <td class="wrapper">${opts.content}${ctaBlock(opts.cta)}</td>
        </tr></table>
        <div class="footer"><table role="presentation" border="0" cellpadding="0" cellspacing="0"><tr>
          <td class="content-block"><span class="apple-link">${BRAND_TAGLINE}</span></td>
        </tr></table></div>
      </div></td>
      <td>&nbsp;</td>
    </tr></table>
  </body>
</html>`;
}
```

> Pozn.: `baseLayout` se v testech Tasku 1 neimportuje přímo, ale render funkce ho používají — `m.html` v testech proto obsahuje `<!doctype html>`.

- [ ] **Step 4: Implementuj `messages.ts`**

`api/src/infrastructure/notify/templates/messages.ts`:

```ts
import { Inquiry } from '../../../domain/inquiry/inquiry';
import { EmailContent, baseLayout, formatCzechDate, publicBaseUrl } from './base';

export { formatCzechDate } from './base';

function stay(inquiry: Inquiry): string {
  return `${formatCzechDate(inquiry.range.arrival)} → ${formatCzechDate(inquiry.range.departure)}`;
}

function greeting(name: string): string {
  return `<p>Dobrý den ${name},</p>`;
}

function stayParagraph(inquiry: Inquiry): string {
  return `<p><strong>Termín pobytu:</strong> ${stay(inquiry)}</p>`;
}

export function inquiryReceivedEmail(inquiry: Inquiry): EmailContent {
  return {
    subject: 'Přijali jsme vaši poptávku',
    text: `Dobrý den ${inquiry.guestName},\nzaznamenali jsme vaši poptávku na termín ${stay(inquiry)} a brzy se vám ozveme s potvrzením dostupnosti.\n\nVinamar`,
    html: baseLayout({
      preheader: 'Vaši poptávku jsme zaznamenali',
      content:
        greeting(inquiry.guestName) +
        '<p>děkujeme za vaši poptávku pobytu v apartmánu Vinamar. Zaznamenali jsme ji a brzy se vám ozveme s potvrzením dostupnosti.</p>' +
        stayParagraph(inquiry),
      cta: { label: 'Zobrazit web', url: publicBaseUrl() },
    }),
  };
}

export function bookingConfirmedEmail(inquiry: Inquiry): EmailContent {
  return {
    subject: 'Vaše rezervace je potvrzena',
    text: `Dobrý den ${inquiry.guestName},\nvaše rezervace pobytu v apartmánu Vinamar na termín ${stay(inquiry)} je závazně potvrzena.\n\nVinamar`,
    html: baseLayout({
      preheader: 'Vaše rezervace je potvrzena',
      content:
        greeting(inquiry.guestName) +
        '<p>vaše rezervace pobytu v apartmánu Vinamar je závazně potvrzena.</p>' +
        stayParagraph(inquiry),
      cta: { label: 'Zobrazit web', url: publicBaseUrl() },
    }),
  };
}

export function inquiryDeclinedEmail(inquiry: Inquiry): EmailContent {
  return {
    subject: 'K vaší poptávce pobytu',
    text: `Dobrý den ${inquiry.guestName},\nděkujeme za zájem o apartmán Vinamar. Termín ${stay(inquiry)} bohužel nemůžeme potvrdit. Podívejte se prosím na další volné termíny.\n\nVinamar`,
    html: baseLayout({
      preheader: 'K vaší poptávce pobytu',
      content:
        greeting(inquiry.guestName) +
        '<p>děkujeme za zájem o apartmán Vinamar. Vámi poptávaný termín bohužel nemůžeme potvrdit.</p>' +
        stayParagraph(inquiry) +
        '<p>Podívejte se prosím na další volné termíny.</p>',
      cta: { label: 'Volné termíny', url: publicBaseUrl() },
    }),
  };
}

export function bookingCancelledEmail(
  inquiry: Inquiry,
  opts: { isOwner: boolean },
): EmailContent {
  if (opts.isOwner) {
    return {
      subject: 'Rezervace byla zrušena',
      text: `Rezervace ${inquiry.guestName} na termín ${stay(inquiry)} byla zrušena a termín se uvolnil.\n\nVinamar`,
      html: baseLayout({
        preheader: 'Rezervace byla zrušena',
        content:
          '<p>Dobrý den,</p>' +
          `<p>rezervace hosta <strong>${inquiry.guestName}</strong> byla zrušena a termín se uvolnil.</p>` +
          stayParagraph(inquiry),
      }),
    };
  }
  return {
    subject: 'Vaše rezervace byla zrušena',
    text: `Dobrý den ${inquiry.guestName},\nvaše rezervace pobytu v apartmánu Vinamar na termín ${stay(inquiry)} byla zrušena.\n\nVinamar`,
    html: baseLayout({
      preheader: 'Vaše rezervace byla zrušena',
      content:
        greeting(inquiry.guestName) +
        '<p>vaše rezervace pobytu v apartmánu Vinamar byla zrušena.</p>' +
        stayParagraph(inquiry),
    }),
  };
}

export function arrivalReminderEmail(inquiry: Inquiry): EmailContent {
  return {
    subject: 'Blíží se váš pobyt',
    text: `Dobrý den ${inquiry.guestName},\nblíží se termín vašeho pobytu v apartmánu Vinamar (${stay(inquiry)}). Těšíme se na vás!\n\nVinamar`,
    html: baseLayout({
      preheader: 'Blíží se váš pobyt',
      content:
        greeting(inquiry.guestName) +
        '<p>blíží se termín vašeho pobytu v apartmánu Vinamar. Těšíme se na vás!</p>' +
        stayParagraph(inquiry),
    }),
  };
}
```

- [ ] **Step 5: Spusť testy, ověř že prochází**

Run: `cd api && npm test -- email-templates`
Expected: PASS (všech 6 testů).

- [ ] **Step 6: Lint + commit**

```bash
cd api && npm run lint
git add api/src/infrastructure/notify/templates api/test/infrastructure/email-templates.spec.ts
git commit -m "feat(notify): HTML e-mail šablony (Postmark basic) jako čisté TS funkce"
```

---

## Task 2: Doménový port `GuestNotifier` + SMTP guest adaptér

**Files:**
- Create: `api/src/domain/inquiry/guest-notifier.port.ts`
- Create: `api/src/infrastructure/notify/smtp-guest-notifier.ts`
- Test: `api/test/infrastructure/smtp-guest-notifier.spec.ts`

**Interfaces:**
- Consumes: render funkce z Task 1 (`inquiryReceivedEmail`, …), `EmailContent`.
- Produces:
  - `const GUEST_NOTIFIER = Symbol('GuestNotifier')`
  - `interface GuestNotifier { inquiryReceived(i: Inquiry): Promise<void>; bookingConfirmed(i: Inquiry): Promise<void>; inquiryDeclined(i: Inquiry): Promise<void>; bookingCancelled(i: Inquiry): Promise<void>; arrivalReminder(i: Inquiry): Promise<void>; }`
  - `class SmtpGuestNotifier implements GuestNotifier` — konstruktor `(@Optional() transport?: Transporter)`.

- [ ] **Step 1: Vytvoř port**

`api/src/domain/inquiry/guest-notifier.port.ts`:

```ts
import { Inquiry } from './inquiry';

export const GUEST_NOTIFIER = Symbol('GuestNotifier');

export interface GuestNotifier {
  inquiryReceived(inquiry: Inquiry): Promise<void>;
  bookingConfirmed(inquiry: Inquiry): Promise<void>;
  inquiryDeclined(inquiry: Inquiry): Promise<void>;
  bookingCancelled(inquiry: Inquiry): Promise<void>;
  arrivalReminder(inquiry: Inquiry): Promise<void>;
}
```

- [ ] **Step 2: Napiš padající test**

`api/test/infrastructure/smtp-guest-notifier.spec.ts`:

```ts
import { Transporter } from 'nodemailer';
import { EmailAddress } from '../../src/domain/shared/email-address';
import { DateRange } from '../../src/domain/shared/date-range';
import { Inquiry } from '../../src/domain/inquiry/inquiry';
import { SmtpGuestNotifier } from '../../src/infrastructure/notify/smtp-guest-notifier';

const inquiry = () =>
  new Inquiry(
    'id-1',
    'Jan Novák',
    new EmailAddress('jan@x.cz'),
    '',
    new DateRange(new Date('2025-07-14'), new Date('2025-07-25')),
    '',
    'confirmed',
    new Date('2025-06-01'),
  );

const fakeTransport = () => {
  const sendMail = jest.fn().mockResolvedValue(undefined);
  return { transport: { sendMail } as unknown as Transporter, sendMail };
};

describe('SmtpGuestNotifier', () => {
  it('sends booking confirmed to the guest email with HTML + text', async () => {
    const { transport, sendMail } = fakeTransport();
    await new SmtpGuestNotifier(transport).bookingConfirmed(inquiry());
    expect(sendMail).toHaveBeenCalledTimes(1);
    const arg = sendMail.mock.calls[0][0];
    expect(arg.to).toBe('jan@x.cz');
    expect(arg.subject).toContain('potvrzena');
    expect(arg.html).toContain('Jan Novák');
    expect(arg.text).toContain('Jan Novák');
  });

  it('sends arrival reminder to the guest', async () => {
    const { transport, sendMail } = fakeTransport();
    await new SmtpGuestNotifier(transport).arrivalReminder(inquiry());
    expect(sendMail.mock.calls[0][0].to).toBe('jan@x.cz');
    expect(sendMail.mock.calls[0][0].subject).toContain('pobyt');
  });
});
```

- [ ] **Step 3: Spusť test, ověř že padá**

Run: `cd api && npm test -- smtp-guest-notifier`
Expected: FAIL — `Cannot find module '.../smtp-guest-notifier'`.

- [ ] **Step 4: Implementuj adaptér**

`api/src/infrastructure/notify/smtp-guest-notifier.ts`:

```ts
import { Injectable, Logger, Optional } from '@nestjs/common';
import { createTransport, Transporter } from 'nodemailer';
import { GuestNotifier } from '../../domain/inquiry/guest-notifier.port';
import { Inquiry } from '../../domain/inquiry/inquiry';
import { EmailContent } from './templates/base';
import {
  inquiryReceivedEmail,
  bookingConfirmedEmail,
  inquiryDeclinedEmail,
  bookingCancelledEmail,
  arrivalReminderEmail,
} from './templates/messages';

@Injectable()
export class SmtpGuestNotifier implements GuestNotifier {
  private readonly logger = new Logger(SmtpGuestNotifier.name);

  constructor(
    @Optional()
    private readonly transport: Transporter = createTransport({
      host: process.env.SMTP_HOST ?? 'mailpit',
      port: Number(process.env.SMTP_PORT ?? 1025),
      secure: false,
    }),
  ) {}

  private async send(to: string, content: EmailContent): Promise<void> {
    await this.transport.sendMail({
      from: {
        name: process.env.MAIL_FROM_NAME ?? 'Vinamar',
        address: process.env.SMTP_FROM ?? 'vinamar@example.com',
      },
      to,
      subject: content.subject,
      html: content.html,
      text: content.text,
    });
  }

  inquiryReceived(inquiry: Inquiry): Promise<void> {
    return this.send(inquiry.email.value, inquiryReceivedEmail(inquiry));
  }
  bookingConfirmed(inquiry: Inquiry): Promise<void> {
    return this.send(inquiry.email.value, bookingConfirmedEmail(inquiry));
  }
  inquiryDeclined(inquiry: Inquiry): Promise<void> {
    return this.send(inquiry.email.value, inquiryDeclinedEmail(inquiry));
  }
  bookingCancelled(inquiry: Inquiry): Promise<void> {
    return this.send(inquiry.email.value, bookingCancelledEmail(inquiry, { isOwner: false }));
  }
  arrivalReminder(inquiry: Inquiry): Promise<void> {
    return this.send(inquiry.email.value, arrivalReminderEmail(inquiry));
  }
}
```

- [ ] **Step 5: Spusť test, ověř že prochází**

Run: `cd api && npm test -- smtp-guest-notifier`
Expected: PASS.

- [ ] **Step 6: Lint + commit**

```bash
cd api && npm run lint
git add api/src/domain/inquiry/guest-notifier.port.ts api/src/infrastructure/notify/smtp-guest-notifier.ts api/test/infrastructure/smtp-guest-notifier.spec.ts
git commit -m "feat(notify): GuestNotifier port + SMTP guest adaptér"
```

---

## Task 3: Guest maily v submit/confirm/decline handlerech

**Files:**
- Modify: `api/test/fakes/index.ts` (přidat `SpyGuestNotifier`)
- Modify: `api/src/application/inquiry/submit-inquiry.handler.ts`
- Modify: `api/src/application/inquiry/confirm-inquiry.handler.ts`
- Modify: `api/src/application/inquiry/decline-inquiry.handler.ts`
- Test: `api/test/application/submit-inquiry.handler.spec.ts`, `confirm-inquiry.handler.spec.ts`

**Interfaces:**
- Consumes: `GuestNotifier`, `GUEST_NOTIFIER` (Task 2).
- Produces (změněné konstruktory — pořadí parametrů je závazné pro pozdější úkoly a testy):
  - `SubmitInquiryHandler(inquiries, availability, ownerNotifier, guestNotifier, clock, idFactory?)`
  - `ConfirmInquiryHandler(inquiries, availability, guestNotifier)`
  - `DeclineInquiryHandler(inquiries, guestNotifier)`
  - `class SpyGuestNotifier implements GuestNotifier` s veřejnými poli `received: { method: string; id: string }[]`.

- [ ] **Step 1: Přidej `SpyGuestNotifier` do fakes**

V `api/test/fakes/index.ts` přidej import a třídu na konec souboru:

```ts
import { GuestNotifier } from '../../src/domain/inquiry/guest-notifier.port';

export class SpyGuestNotifier implements GuestNotifier {
  received: { method: string; id: string }[] = [];
  async inquiryReceived(i: Inquiry): Promise<void> { this.received.push({ method: 'inquiryReceived', id: i.id }); }
  async bookingConfirmed(i: Inquiry): Promise<void> { this.received.push({ method: 'bookingConfirmed', id: i.id }); }
  async inquiryDeclined(i: Inquiry): Promise<void> { this.received.push({ method: 'inquiryDeclined', id: i.id }); }
  async bookingCancelled(i: Inquiry): Promise<void> { this.received.push({ method: 'bookingCancelled', id: i.id }); }
  async arrivalReminder(i: Inquiry): Promise<void> { this.received.push({ method: 'arrivalReminder', id: i.id }); }
}
```

- [ ] **Step 2: Napiš/uprav padající testy**

V `api/test/application/submit-inquiry.handler.spec.ts` uprav `make()` a doplň asserty:

```ts
import { FixedClock, InMemoryAvailability, InMemoryInquiries, SpyNotifier, SpyGuestNotifier } from '../fakes';

const make = () => {
  const availability = new InMemoryAvailability();
  const inquiries = new InMemoryInquiries();
  const notifier = new SpyNotifier();
  const guest = new SpyGuestNotifier();
  const clock = new FixedClock(new Date('2026-01-01'));
  const handler = new SubmitInquiryHandler(inquiries, availability, notifier, guest, clock, () => 'id-1');
  return { handler, availability, inquiries, notifier, guest };
};
```

Do existujícího testu „persists a pending inquiry and notifies the owner" přidej:

```ts
    expect(guest.received).toEqual([{ method: 'inquiryReceived', id: 'id-1' }]);
```

Do testu „admin booking: short stay is auto-confirmed…" přidej:

```ts
    expect(guest.received).toEqual([{ method: 'bookingConfirmed', id: 'id-1' }]);
```

V `api/test/application/confirm-inquiry.handler.spec.ts` uprav konstrukce handlerů: `SubmitInquiryHandler(..., new SpyGuestNotifier(), clock, () => id)` a `new ConfirmInquiryHandler(inquiries, availability, guest)`, kde `guest = new SpyGuestNotifier()`. Do testu auto-decline přidej:

```ts
    expect(guest.received).toContainEqual({ method: 'bookingConfirmed', id: 'winner' });
    expect(guest.received).toContainEqual({ method: 'inquiryDeclined', id: 'overlap' });
    expect(guest.received.filter((r) => r.method === 'inquiryDeclined')).toHaveLength(1);
```

(Konkrétně: nahraď `const notifier = new SpyNotifier();` přidáním `const guest = new SpyGuestNotifier();`, v `submitWith` předej `guest` jako 4. argument a `new ConfirmInquiryHandler(inquiries, availability, guest)`.)

- [ ] **Step 3: Spusť testy, ověř že padají**

Run: `cd api && npm test -- inquiry.handler`
Expected: FAIL — špatný počet argumentů konstruktoru / `guest.received` undefined.

- [ ] **Step 4: Uprav `submit-inquiry.handler.ts`**

Přidej importy a parametr, pošli guest maily (best-effort). Konkrétně:

```ts
import { GUEST_NOTIFIER, GuestNotifier } from '../../domain/inquiry/guest-notifier.port';
```

Konstruktor (přidat `guest` za `notifier`):

```ts
  constructor(
    @Inject(INQUIRY_REPOSITORY) private readonly inquiries: InquiryRepository,
    @Inject(AVAILABILITY_REPOSITORY) private readonly availability: AvailabilityRepository,
    @Inject(OWNER_NOTIFIER) private readonly notifier: OwnerNotifier,
    @Inject(GUEST_NOTIFIER) private readonly guest: GuestNotifier,
    @Inject(CLOCK) private readonly clock: Clock,
    @Optional() private readonly idFactory: () => string = randomUUID,
  ) {}
```

V admin větvi před `return { id: inquiry.id }` přidej:

```ts
      await this.safeGuest(() => this.guest.bookingConfirmed(inquiry), inquiry.id);
      return { id: inquiry.id };
```

V public větvi za stávající owner `try/catch` přidej:

```ts
    await this.safeGuest(() => this.guest.inquiryReceived(inquiry), inquiry.id);
```

A přidej privátní helper:

```ts
  private async safeGuest(fn: () => Promise<void>, id: string): Promise<void> {
    try {
      await fn();
    } catch (err) {
      this.logger.warn(`guest notification failed for inquiry ${id}: ${String(err)}`);
    }
  }
```

- [ ] **Step 5: Uprav `confirm-inquiry.handler.ts`**

Přidej import a parametr konstruktoru `@Inject(GUEST_NOTIFIER) private readonly guest: GuestNotifier`, `Logger` a helper `safeGuest` (stejný jako výše). Po `updateStatus(cmd.id, 'confirmed')` a `availability.save(...)`:

```ts
    await this.safeGuest(() => this.guest.bookingConfirmed(inquiry), inquiry.id);
```

V auto-decline smyčce za `updateStatus(other.id, 'declined')`:

```ts
        await this.safeGuest(() => this.guest.inquiryDeclined(other), other.id);
```

Helper:

```ts
  private readonly logger = new Logger(ConfirmInquiryHandler.name);
  private async safeGuest(fn: () => Promise<void>, id: string): Promise<void> {
    try { await fn(); } catch (err) { this.logger.warn(`guest notification failed for inquiry ${id}: ${String(err)}`); }
  }
```

(Importy: `import { Inject, Logger } from '@nestjs/common';` a `import { GUEST_NOTIFIER, GuestNotifier } from '../../domain/inquiry/guest-notifier.port';`.)

- [ ] **Step 6: Uprav `decline-inquiry.handler.ts`**

```ts
import { Inject, Logger } from '@nestjs/common';
import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { DeclineInquiryCommand } from './decline-inquiry.command';
import { INQUIRY_REPOSITORY, InquiryRepository } from '../../domain/inquiry/inquiry.repository.port';
import { GUEST_NOTIFIER, GuestNotifier } from '../../domain/inquiry/guest-notifier.port';

@CommandHandler(DeclineInquiryCommand)
export class DeclineInquiryHandler implements ICommandHandler<DeclineInquiryCommand> {
  private readonly logger = new Logger(DeclineInquiryHandler.name);
  constructor(
    @Inject(INQUIRY_REPOSITORY) private readonly inquiries: InquiryRepository,
    @Inject(GUEST_NOTIFIER) private readonly guest: GuestNotifier,
  ) {}

  async execute(cmd: DeclineInquiryCommand): Promise<void> {
    const inquiry = await this.inquiries.get(cmd.id);
    await this.inquiries.updateStatus(cmd.id, 'declined');
    if (inquiry) {
      try {
        await this.guest.inquiryDeclined(inquiry);
      } catch (err) {
        this.logger.warn(`guest notification failed for inquiry ${cmd.id}: ${String(err)}`);
      }
    }
  }
}
```

- [ ] **Step 7: Spusť testy, ověř že prochází**

Run: `cd api && npm test -- inquiry.handler`
Expected: PASS.

- [ ] **Step 8: Lint + commit**

```bash
cd api && npm run lint
git add api/src/application/inquiry api/test/fakes/index.ts api/test/application/submit-inquiry.handler.spec.ts api/test/application/confirm-inquiry.handler.spec.ts
git commit -m "feat(inquiry): guest e-maily při poptávce, potvrzení a odmítnutí"
```

---

## Task 4: Maily o zrušení (host + majitel)

**Files:**
- Modify: `api/src/domain/inquiry/owner-notifier.port.ts`
- Modify: `api/src/infrastructure/notify/smtp-owner-notifier.ts`
- Modify: `api/test/fakes/index.ts` (rozšířit `SpyNotifier`)
- Modify: `api/src/application/availability/cancel-calendar-entry.handler.ts`
- Test: `api/test/application/cancel-calendar-entry.handler.spec.ts`

**Interfaces:**
- Consumes: `GuestNotifier` (Task 2), `bookingCancelledEmail` (Task 1).
- Produces:
  - `OwnerNotifier.bookingCancelled(inquiry: Inquiry): Promise<void>`
  - `CancelCalendarEntryHandler(availability, inquiries, guestNotifier, ownerNotifier)`
  - `SpyNotifier.cancelled: Inquiry[]`

- [ ] **Step 1: Rozšiř port `OwnerNotifier`**

`api/src/domain/inquiry/owner-notifier.port.ts`:

```ts
export interface OwnerNotifier {
  inquiryReceived(inquiry: Inquiry): Promise<void>;
  bookingCancelled(inquiry: Inquiry): Promise<void>;
}
```

- [ ] **Step 2: Rozšiř `SpyNotifier` ve fakes**

V `api/test/fakes/index.ts` ve třídě `SpyNotifier`:

```ts
export class SpyNotifier implements OwnerNotifier {
  received: Inquiry[] = [];
  cancelled: Inquiry[] = [];
  async inquiryReceived(inquiry: Inquiry): Promise<void> { this.received.push(inquiry); }
  async bookingCancelled(inquiry: Inquiry): Promise<void> { this.cancelled.push(inquiry); }
}
```

- [ ] **Step 3: Napiš padající test**

V `api/test/application/cancel-calendar-entry.handler.spec.ts` uprav obě konstrukce na `new CancelCalendarEntryHandler(availability, inquiries, guest, owner)` a v prvním testu („frees the term…") přidej spies a asserty:

```ts
import { FixedClock, InMemoryAvailability, InMemoryInquiries, SpyNotifier, SpyGuestNotifier } from '../fakes';
// ...
    const guest = new SpyGuestNotifier();
    const owner = new SpyNotifier();
    const cancel = new CancelCalendarEntryHandler(availability, inquiries, guest, owner);
    await cancel.execute(new CancelCalendarEntryCommand(blockId));

    expect((await inquiries.get('id-1'))!.status).toBe('cancelled');
    expect(guest.received).toEqual([{ method: 'bookingCancelled', id: 'id-1' }]);
    expect(owner.cancelled.map((i) => i.id)).toEqual(['id-1']);
```

A v druhém testu („no linked inquiry") konstrukci `new CancelCalendarEntryHandler(availability, inquiries, new SpyGuestNotifier(), new SpyNotifier())` a assert, že se neposlalo nic (žádný `inquiryId`).

- [ ] **Step 4: Spusť test, ověř že padá**

Run: `cd api && npm test -- cancel-calendar-entry`
Expected: FAIL — špatný počet argumentů / `guest.received` undefined.

- [ ] **Step 5: Uprav `cancel-calendar-entry.handler.ts`**

```ts
import { Inject, Logger } from '@nestjs/common';
import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { CancelCalendarEntryCommand } from './cancel-calendar-entry.command';
import { AVAILABILITY_REPOSITORY, AvailabilityRepository } from '../../domain/availability/availability.repository.port';
import { INQUIRY_REPOSITORY, InquiryRepository } from '../../domain/inquiry/inquiry.repository.port';
import { GUEST_NOTIFIER, GuestNotifier } from '../../domain/inquiry/guest-notifier.port';
import { OWNER_NOTIFIER, OwnerNotifier } from '../../domain/inquiry/owner-notifier.port';

@CommandHandler(CancelCalendarEntryCommand)
export class CancelCalendarEntryHandler implements ICommandHandler<CancelCalendarEntryCommand> {
  private readonly logger = new Logger(CancelCalendarEntryHandler.name);
  constructor(
    @Inject(AVAILABILITY_REPOSITORY) private readonly availability: AvailabilityRepository,
    @Inject(INQUIRY_REPOSITORY) private readonly inquiries: InquiryRepository,
    @Inject(GUEST_NOTIFIER) private readonly guest: GuestNotifier,
    @Inject(OWNER_NOTIFIER) private readonly owner: OwnerNotifier,
  ) {}

  async execute(cmd: CancelCalendarEntryCommand): Promise<void> {
    const { inquiryId } = await this.availability.delete(cmd.id);
    if (!inquiryId) {
      return;
    }
    await this.inquiries.updateStatus(inquiryId, 'cancelled');
    const inquiry = await this.inquiries.get(inquiryId);
    if (!inquiry) {
      return;
    }
    try {
      await this.guest.bookingCancelled(inquiry);
      await this.owner.bookingCancelled(inquiry);
    } catch (err) {
      this.logger.warn(`cancellation notification failed for inquiry ${inquiryId}: ${String(err)}`);
    }
  }
}
```

- [ ] **Step 6: Doplň `bookingCancelled` do `smtp-owner-notifier.ts`**

Přidej import a metodu (host = plain text zůstává; majitel dostane HTML šablonu):

```ts
import { bookingCancelledEmail } from './templates/messages';
// ... uvnitř třídy:
  async bookingCancelled(inquiry: Inquiry): Promise<void> {
    const content = bookingCancelledEmail(inquiry, { isOwner: true });
    await this.transport.sendMail({
      from: process.env.SMTP_FROM ?? 'vinamar@example.com',
      to: process.env.OWNER_EMAIL ?? 'owner@example.com',
      subject: content.subject,
      html: content.html,
      text: content.text,
    });
  }
```

- [ ] **Step 7: Spusť testy, ověř že prochází**

Run: `cd api && npm test -- cancel-calendar-entry`
Expected: PASS.

- [ ] **Step 8: Lint + commit**

```bash
cd api && npm run lint
git add api/src/domain/inquiry/owner-notifier.port.ts api/src/infrastructure/notify/smtp-owner-notifier.ts api/test/fakes/index.ts api/src/application/availability/cancel-calendar-entry.handler.ts api/test/application/cancel-calendar-entry.handler.spec.ts
git commit -m "feat(inquiry): e-maily o zrušení rezervace hostovi i majiteli"
```

---

## Task 5: Připomínka 14 dní před příjezdem (migrace + repo + handler + cron)

**Files:**
- Create: `api/migrations/1700000006000_inquiry-arrival-reminder.sql`
- Modify: `api/src/domain/inquiry/inquiry.repository.port.ts`
- Modify: `api/src/infrastructure/persistence/pg-inquiry.repository.ts`
- Modify: `api/test/fakes/index.ts` (`InMemoryInquiries`)
- Create: `api/src/application/inquiry/send-arrival-reminders.command.ts`
- Create: `api/src/application/inquiry/send-arrival-reminders.handler.ts`
- Create: `api/src/infrastructure/notify/arrival-reminder.cron.ts`
- Test: `api/test/application/send-arrival-reminders.handler.spec.ts`

**Interfaces:**
- Consumes: `GuestNotifier`, `Clock` (`CLOCK`), `InquiryRepository`.
- Produces:
  - `InquiryRepository.listDueForArrivalReminder(now: Date): Promise<Inquiry[]>`
  - `InquiryRepository.markArrivalReminderSent(id: string): Promise<void>`
  - `class SendArrivalRemindersCommand {}` (bez parametrů)
  - `class SendArrivalRemindersHandler` — konstruktor `(inquiries, guest, clock)`.

- [ ] **Step 1: Migrace**

`api/migrations/1700000006000_inquiry-arrival-reminder.sql`:

```sql
-- Up Migration
ALTER TABLE inquiries ADD COLUMN arrival_reminder_sent_at timestamptz;

-- Down Migration
ALTER TABLE inquiries DROP COLUMN arrival_reminder_sent_at;
```

- [ ] **Step 2: Rozšiř repository port**

`api/src/domain/inquiry/inquiry.repository.port.ts` — do interface přidej:

```ts
  listDueForArrivalReminder(now: Date): Promise<Inquiry[]>;
  markArrivalReminderSent(id: string): Promise<void>;
```

- [ ] **Step 3: Rozšiř `InMemoryInquiries` ve fakes**

V `api/test/fakes/index.ts` do třídy `InMemoryInquiries` přidej pole a metody:

```ts
  reminderSent = new Set<string>();
  async listDueForArrivalReminder(now: Date): Promise<Inquiry[]> {
    const max = new Date(now.getTime() + 14 * 86400000);
    return this.items.filter(
      (i) =>
        i.status === 'confirmed' &&
        !this.reminderSent.has(i.id) &&
        i.range.arrival.getTime() > now.getTime() &&
        i.range.arrival.getTime() <= max.getTime(),
    );
  }
  async markArrivalReminderSent(id: string): Promise<void> {
    this.reminderSent.add(id);
  }
```

- [ ] **Step 4: Napiš padající test**

`api/test/application/send-arrival-reminders.handler.spec.ts`:

```ts
import { SendArrivalRemindersHandler } from '../../src/application/inquiry/send-arrival-reminders.handler';
import { SendArrivalRemindersCommand } from '../../src/application/inquiry/send-arrival-reminders.command';
import { EmailAddress } from '../../src/domain/shared/email-address';
import { DateRange } from '../../src/domain/shared/date-range';
import { Inquiry, InquiryStatus } from '../../src/domain/inquiry/inquiry';
import { FixedClock, InMemoryInquiries, SpyGuestNotifier } from '../fakes';

const NOW = new Date('2026-01-01T00:00:00Z');
const at = (status: InquiryStatus, id: string, arrivalDaysFromNow: number) =>
  new Inquiry(
    id,
    'Host',
    new EmailAddress(`${id}@x.cz`),
    '',
    new DateRange(
      new Date(NOW.getTime() + arrivalDaysFromNow * 86400000),
      new Date(NOW.getTime() + (arrivalDaysFromNow + 7) * 86400000),
    ),
    '',
    status,
    NOW,
  );

const make = async (seed: Inquiry[]) => {
  const inquiries = new InMemoryInquiries();
  for (const i of seed) await inquiries.save(i);
  const guest = new SpyGuestNotifier();
  const handler = new SendArrivalRemindersHandler(inquiries, guest, new FixedClock(NOW));
  return { inquiries, guest, handler };
};

describe('SendArrivalRemindersHandler', () => {
  it('reminds confirmed stays arriving within 14 days, once', async () => {
    const { guest, handler, inquiries } = await make([
      at('confirmed', 'soon', 10), // within window
      at('confirmed', 'edge', 14), // inclusive edge
      at('confirmed', 'far', 20), // outside window
      at('pending', 'pending', 10), // not confirmed
      at('confirmed', 'past', -1), // already arrived
    ]);

    await handler.execute(new SendArrivalRemindersCommand());

    const reminded = guest.received.filter((r) => r.method === 'arrivalReminder').map((r) => r.id).sort();
    expect(reminded).toEqual(['edge', 'soon']);
    expect(inquiries.reminderSent.has('soon')).toBe(true);

    // second run is idempotent — no duplicate reminders
    guest.received = [];
    await handler.execute(new SendArrivalRemindersCommand());
    expect(guest.received).toHaveLength(0);
  });

  it('leaves the inquiry unmarked when sending fails, so it retries next run', async () => {
    const { handler, inquiries, guest } = await make([at('confirmed', 'soon', 10)]);
    guest.arrivalReminder = async () => {
      throw new Error('smtp down');
    };
    await handler.execute(new SendArrivalRemindersCommand());
    expect(inquiries.reminderSent.has('soon')).toBe(false);
  });
});
```

- [ ] **Step 5: Spusť test, ověř že padá**

Run: `cd api && npm test -- send-arrival-reminders`
Expected: FAIL — `Cannot find module '.../send-arrival-reminders.handler'`.

- [ ] **Step 6: Implementuj command + handler**

`api/src/application/inquiry/send-arrival-reminders.command.ts`:

```ts
export class SendArrivalRemindersCommand {}
```

`api/src/application/inquiry/send-arrival-reminders.handler.ts`:

```ts
import { Inject, Logger } from '@nestjs/common';
import { CommandHandler, ICommandHandler } from '@nestjs/cqrs';
import { SendArrivalRemindersCommand } from './send-arrival-reminders.command';
import { INQUIRY_REPOSITORY, InquiryRepository } from '../../domain/inquiry/inquiry.repository.port';
import { GUEST_NOTIFIER, GuestNotifier } from '../../domain/inquiry/guest-notifier.port';
import { CLOCK, Clock } from '../../domain/shared/clock.port';

@CommandHandler(SendArrivalRemindersCommand)
export class SendArrivalRemindersHandler implements ICommandHandler<SendArrivalRemindersCommand> {
  private readonly logger = new Logger(SendArrivalRemindersHandler.name);
  constructor(
    @Inject(INQUIRY_REPOSITORY) private readonly inquiries: InquiryRepository,
    @Inject(GUEST_NOTIFIER) private readonly guest: GuestNotifier,
    @Inject(CLOCK) private readonly clock: Clock,
  ) {}

  async execute(): Promise<void> {
    const due = await this.inquiries.listDueForArrivalReminder(this.clock.now());
    for (const inquiry of due) {
      try {
        await this.guest.arrivalReminder(inquiry);
        await this.inquiries.markArrivalReminderSent(inquiry.id);
      } catch (err) {
        // Leave unmarked so the next daily run retries.
        this.logger.warn(`arrival reminder failed for inquiry ${inquiry.id}: ${String(err)}`);
      }
    }
  }
}
```

- [ ] **Step 7: Implementuj PG repository metody**

V `api/src/infrastructure/persistence/pg-inquiry.repository.ts` přidej:

```ts
  async listDueForArrivalReminder(now: Date): Promise<Inquiry[]> {
    const { rows } = await this.pool.query(
      `SELECT * FROM inquiries
       WHERE status = 'confirmed'
         AND arrival_reminder_sent_at IS NULL
         AND arrival > $1::date
         AND arrival <= $1::date + 14
       ORDER BY arrival ASC`,
      [now],
    );
    return rows.map((r) => this.toInquiry(r));
  }

  async markArrivalReminderSent(id: string): Promise<void> {
    await this.pool.query(
      `UPDATE inquiries SET arrival_reminder_sent_at = now() WHERE id = $1`,
      [id],
    );
  }
```

- [ ] **Step 8: Implementuj cron**

`api/src/infrastructure/notify/arrival-reminder.cron.ts` (vzor `flight-price.cron.ts`, ale BEZ `onApplicationBootstrap` — připomínky se nespouští při každém startu):

```ts
import { Injectable } from '@nestjs/common';
import { Cron, CronExpression } from '@nestjs/schedule';
import { CommandBus } from '@nestjs/cqrs';
import { SendArrivalRemindersCommand } from '../../application/inquiry/send-arrival-reminders.command';

@Injectable()
export class ArrivalReminderCron {
  constructor(private readonly commandBus: CommandBus) {}

  @Cron(CronExpression.EVERY_DAY_AT_8AM)
  async daily(): Promise<void> {
    await this.commandBus.execute(new SendArrivalRemindersCommand());
  }
}
```

- [ ] **Step 9: Spusť test, ověř že prochází**

Run: `cd api && npm test -- send-arrival-reminders`
Expected: PASS (oba testy).

- [ ] **Step 10: Lint + commit**

```bash
cd api && npm run lint
git add api/migrations/1700000006000_inquiry-arrival-reminder.sql api/src/domain/inquiry/inquiry.repository.port.ts api/src/infrastructure/persistence/pg-inquiry.repository.ts api/test/fakes/index.ts api/src/application/inquiry/send-arrival-reminders.command.ts api/src/application/inquiry/send-arrival-reminders.handler.ts api/src/infrastructure/notify/arrival-reminder.cron.ts api/test/application/send-arrival-reminders.handler.spec.ts
git commit -m "feat(inquiry): připomínka 14 dní před příjezdem (cron + idempotence)"
```

---

## Task 6: DI wiring, env a manuální ověření mailpitem

**Files:**
- Modify: `api/src/interface/inquiry.module.ts`
- Modify: `docker-compose.yml`
- Modify: `.env.example`
- Modify: `README.md`

**Interfaces:**
- Consumes: vše z Task 2–5.

- [ ] **Step 1: Zaregistruj providery v `inquiry.module.ts`**

Přidej importy a do `providers` přidej `SmtpGuestNotifier`, mapování `GUEST_NOTIFIER`, `SendArrivalRemindersHandler` a `ArrivalReminderCron`:

```ts
import { SmtpGuestNotifier } from '../infrastructure/notify/smtp-guest-notifier';
import { SendArrivalRemindersHandler } from '../application/inquiry/send-arrival-reminders.handler';
import { ArrivalReminderCron } from '../infrastructure/notify/arrival-reminder.cron';
import { GUEST_NOTIFIER } from '../domain/inquiry/guest-notifier.port';
```

V `providers` přidej:

```ts
    SendArrivalRemindersHandler,
    ArrivalReminderCron,
    { provide: GUEST_NOTIFIER, useClass: SmtpGuestNotifier },
```

> **Pozn. k cronu:** `ScheduleModule.forRoot()` je už registrován ve `FlightModule`. Explorer `@nestjs/schedule` skenuje providery napříč celou aplikací, takže `ArrivalReminderCron` se objeví bez dalšího `forRoot()`. **Nepřidávej** druhý `ScheduleModule.forRoot()` — dvojitá registrace by mohla cron spustit dvakrát.

- [ ] **Step 2: Přidej env proměnné do `docker-compose.yml`**

Ve službě `api` v sekci `environment` přidej:

```yaml
      MAIL_FROM_NAME: ${MAIL_FROM_NAME:-Vinamar}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-http://localhost:3000}
```

- [ ] **Step 3: Přidej do `.env.example`**

```bash
MAIL_FROM_NAME=Vinamar
PUBLIC_BASE_URL=http://localhost:3000
```

- [ ] **Step 4: Zdokumentuj v `README.md`**

Do sekce o e-mailech / env doplň krátkou poznámku, že transakční maily používají `SMTP_*`, `MAIL_FROM_NAME`, `OWNER_EMAIL` a odkazy staví z `PUBLIC_BASE_URL`; v dev prostředí je zachytává mailpit.

- [ ] **Step 5: Build + plný test suite (regresní kontrola)**

```bash
cd api && npm run build && npm run lint && npm test
```
Expected: build OK, lint OK, všechny unit testy PASS.

- [ ] **Step 6: Manuální ověření mailpitem**

```bash
# z kořene worktree
docker compose up -d --build db api web mailpit
```
1. Otevři web na `http://localhost:3100`, odešli poptávku (≥ 7 nocí, termín v budoucnu).
2. Otevři mailpit UI na `http://localhost:8126` — ověř, že dorazily **dva** maily: potvrzení hostovi („Přijali jsme vaši poptávku") a notifikace majiteli.
3. V administraci poptávku potvrď → hostovi dorazí „Vaše rezervace je potvrzena".
4. Zruš rezervaci v kalendáři → hostovi i majiteli dorazí mail o zrušení.
5. Zkontroluj, že HTML maily se v mailpitu zobrazují s hlavičkou „Vinamar" a správným termínem v českém formátu.

> Automatizovaný mailpit e2e není součástí (projekt nemá mailpit test-harness; DB e2e jsou gated `RUN_DB_INTEGRATION=1`). Doručení a obsah pokrývají unit testy adaptéru (Task 2) a šablon (Task 1); tento krok je manuální smoke.

- [ ] **Step 7: Commit**

```bash
git add api/src/interface/inquiry.module.ts docker-compose.yml .env.example README.md
git commit -m "feat(notify): zapojení guest notifieru a cronu připomínek + env"
```

---

## Pořadí a závislosti úkolů

1 (šablony) → 2 (port + adaptér) → 3 (submit/confirm/decline) → 4 (zrušení) → 5 (připomínka) → 6 (wiring). Task 3 a 4 oba mění `test/fakes/index.ts` a měly by jít sériově (4 staví na `SpyGuestNotifier` z 3). Task 5 je nezávislý na 3/4 kromě sdíleného `GUEST_NOTIFIER` (Task 2), ale řadíme ho po 4 kvůli pořadí commitů.

## Mimo rozsah

- Příloha smlouvy (PDF) v mailu „Rezervace potvrzena" — bod **I** se napojí na `guest.bookingConfirmed`.
- Finální logo/barvy — bod **L** naplní konstanty v `base.ts`.
- Jiné jazyky než čeština, inlining CSS, fronta/retry odesílání.
